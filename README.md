* make sure to use raspberry pi 3 with Buster OS
* inside raspi-config turn on camera in interfacing
* inside raspi-config make filesystem read only after editing code(important to prevent data loss)
* you can re enable writing in raspi config